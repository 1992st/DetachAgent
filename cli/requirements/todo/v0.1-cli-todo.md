# Detach Agent CLI Todo v0.1

## Reserved

- Keep `detach-agent config` reserved for future design.
- Do not add `config profile list/use` in v0.1.
- Do not add app/server `serve` commands in v0.1.
- Do not add standalone Gateway client mode in any future version.
- Do not make CLI start Detach Agent App/server.

## Future Questions

- Whether CLI should later support sending files together with messages.
- Whether profile management belongs in CLI at all, or should remain app-only.
