# Remote Control Design

Remote control is intentionally reserved for the next phase.

Required guardrails:

- explicit user approval
- audit log
- command timeout
- least-privilege account
- workspace/path restrictions
- output redaction

The first version exposes a placeholder UI panel and a server-side `control` service module.
